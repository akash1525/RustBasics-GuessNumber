To run this project you must have Rust Installed. Also have cargo installed.  
#https://www.rust-lang.org/
Run rustup update
check cargo version by cargo --version
in root directory, run the command -> cargo run